{
  "name": "@ctok/action",
  "version": "0.1.0",
  "description": "GitHub Action - ctok Claude token estimator",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "node esbuild.js",
    "dev": "node esbuild.js --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@ctok/core": "workspace:*",
    "@ctok/quota": "workspace:*",
    "@ctok/refiner": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
