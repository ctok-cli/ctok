name: Slack Bot — CI

on:
  push:
    branches: [main]
    paths:
      - "apps/slack/**"
      - "packages/core/**"
      - "packages/refiner/**"
      - "packages/quota/**"
      - "packages/scanner/**"
  pull_request:
    paths:
      - "apps/slack/**"

jobs:
  build-test:
    name: Typecheck & test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build workspace packages
        run: |
          pnpm --filter @ctok/core build
          pnpm --filter @ctok/refiner build
          pnpm --filter @ctok/quota build
          pnpm --filter @ctok/scanner build

      - name: Typecheck
        run: pnpm --filter @ctok/slack typecheck

      - name: Test
        run: pnpm --filter @ctok/slack test

      - name: Build
        run: pnpm --filter @ctok/slack build

      - name: Verify bundle
        run: test -f apps/slack/dist/index.js
