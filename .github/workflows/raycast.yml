name: Raycast Extension - CI

on:
  push:
    branches: [main]
    paths:
      - "apps/raycast/**"
      - "packages/core/**"
      - "packages/refiner/**"
      - "packages/quota/**"
      - "packages/scanner/**"
  pull_request:
    paths:
      - "apps/raycast/**"

jobs:
  build-test:
    name: Typecheck & test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build workspace packages
        run: |
          pnpm --filter @ctok/core build
          pnpm --filter @ctok/refiner build
          pnpm --filter @ctok/quota build
          pnpm --filter @ctok/scanner build

      - name: Typecheck
        run: pnpm --filter @ctok/raycast typecheck

      - name: Test
        run: pnpm --filter @ctok/raycast test
